package com.example.s3transfer.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class OpenApiConfig {
    // Keep this class intentionally empty to avoid IDE/compiler import issues.
    // Default springdoc behavior (springdoc-openapi-ui) will expose /v3/api-docs and the Swagger UI.
}
